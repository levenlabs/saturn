syntax = "proto3";

package proto;

message ReportRequest {
  int64 time = 1; // time
  string name = 2; // name of the server
  string reply = 3; // reply address
  int32 seq = 4;
  bytes sig = 5; // hmac of time + name + reply + seq separated by :
}
